/**
 * array - data structure which contains different elements
 * the elements can be of different data types
 *
 * syntax - const array = [elemet1, ele2 ...... elemn]
 * to access an element we use indices or index
 * element1 =  array[0]
 * element2  = array[1]
 */


const politicians = ['Ruto', 'Uhuru', 'Moi', 'Kibaki']
//          positions 1        2        3        4
//              index 0         1       2        3
// all politicians
console.log(politicians);
// Ruto
console.log(politicians[0]);


const east_leigh_cars = ['mercedes', 'toyota', 40, 60, 'volvo', 'bmw'];
console.log(`car 1 is ${east_leigh_cars[2]}`);
